<% turn = locals[:turn] %>

  <span>
  <%= h me.world.playing_player.name %>
  </span>
  <hr>
  <ul class="phase">
  <% @world.turn.phases.each do |phase| %>
    <li class="<%= 'selected' if phase == turn.phase %>" ><%= phase %></li>
  <% end %>
  </ul>
