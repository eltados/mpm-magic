<% turn = locals[:turn] %>

  <span class="<%= "playing" if me.playing? %>">
  <%= h $world.playing_player.name %>
  </span>
  <hr>
  <ul class="phase">
  <% Turn.phases.each do |phase| %>
    <li class="<%= 'selected' if phase == turn.phase %>" ><%= phase %></li>
  <% end %>
  </ul>
